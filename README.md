# Build
```bash
yarn set version berry
yarn
yarn run tsc --build
```

# Workspaces
List workspaces
```bash
yarn workspaces list
```

Build app-a
```bash
yarn workspace app-a tsc
```

Build app-a
```bash
yarn workspace app-b tsc
```